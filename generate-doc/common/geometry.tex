% Page layout (geometry)
\usepackage[
    a4paper,
    portrait,
    margin=.8in,
    top=.5in,
    headheight=60pt,
    includeheadfoot, % include headerheight in pagesize calculations
    % showframe
]{geometry}
